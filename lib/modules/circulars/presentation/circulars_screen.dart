import 'package:flutter/material.dart';

class CircularsScreen extends StatelessWidget {
  const CircularsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('circulars')),
    );
  }
}
